enum ChannelPrivacy {
  public,
  private,
}

enum ReactionType {
  like,
  love,
  haha,
  seen,
  completed,
  dislike,
}

enum TaskStatus {
  inProgress,
  completed,
  overdue,
  overdueCompleted,
}

enum SearchThreadFilter {
  all,
  // unread,
  myThreads,
  // tagged,
}

enum StatusType {
  error,
  info,
  warning,
  success,
}

enum ThreadType {
  message,
  event,
  milestone,
}
